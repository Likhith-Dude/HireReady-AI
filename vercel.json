{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "echo 'install handled in buildCommand'",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://hireready-api-cifu.onrender.com"
  }
}
